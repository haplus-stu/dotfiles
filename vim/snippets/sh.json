{
  "check_OS":{
    "prefix":["OS"],
    body:[
      "if [[ "$(uname)" == "Darwin"  ]]; then
        export OS="Mac"
        else
          export OS="Linux"
       fi"
    ],
    "description":"check OS."
  }
}
